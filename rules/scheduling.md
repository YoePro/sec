# Scheduling

## Purpose

Scheduling defines how tasks and threads make progress on a selected target.

This rulebook is planned and does not define final syntax.

It must cover:

- task scheduler selection
- physical thread scheduling constraints
- fairness and starvation policies
- target profile limits
- executor integration
- no-required-runtime targets

No rule here implies that a task is equivalent to a physical thread.
