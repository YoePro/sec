#target(os: "linux", arch: "any")
module linux

import "platform/linux/amd64"

unsafe fn Exit(code: int) never {
    amd64.RawSyscall(uint(amd64.SysCall.sysExit), uint(code))
}

// unsafe fn Exit(code: int) never
// fn ProcessID() int
// fn ParentProcessID() int
